/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['smasitalizzah.sch.id', 'images.unsplash.com', 'storage.googleapis.com'],
      },
};

export default nextConfig;
